language: python
python:
  - "2.7"
before_install:
  - sudo apt-get update -qq
  - sudo apt-get -y build-dep python-rrdtool
install:
  - pip install -r requirements.txt
  - pip install nose
script:
  - nosetests --with-coverage --cover-package=db,graphs,rrd,settings,tests